import { styled } from '@mui/material';
// import BackgroundImage from '/br.webp';

export const RootStyle = styled('div')({
	// overflow: 'hidden',
	position: 'relative',
	// maxWidth: '100vw',
});

export const MainStyle = styled('div')({
	// flexGrow: 1,
	minHeight: '100vh',
	// width: '100vw',
});
